{
  "name": "finished-games",
  "version": "1.0.1",
  "description": "",
  "main": "index.js",
  "type": "module",
  "private": true,
  "scripts": {
    "storybook": "npm --prefix client run storybook",
    "serverBuild": "npm --prefix server run build",
    "serverStart": "npm --prefix server run start",
    "clientBuild": "npm --prefix client run build",
    "clientStart": "npm --prefix client run start",
    "installDependencies": "npm install && npm --prefix server install && npm --prefix client install",
    "build": "npm run serverBuild && npm run clientBuild && concurrently \"npm run serverStart\" \"npm run clientStart\""
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/a1Thiago/finished-games.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/a1Thiago/finished-games/issues"
  },
  "homepage": "https://github.com/a1Thiago/finished-games#readme",
  "devDependencies": {
    "concurrently": "^8.0.1",
    "typescript": "^4.9.3",
    "vite": "^4.2.0"
  }
}